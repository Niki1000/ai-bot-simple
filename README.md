# 🤖 AI Dating Bot

Telegram бот с виртуальными персонажами и Web App интерфейсом.

## 🚀 Особенности

- **Telegram бот** с командами и меню
- **Web App** интерфейс для выбора персонажей и чата
- **5 уникальных персонажей** с разными характерами
- **База данных MongoDB** для хранения пользователей и статистики
- **Уровни доверия** и система сообщений
- **Полностью готово к деплою** на Vercel

## 📦 Установка и запуск

### Тестовый endpoint подписки (до внедрения оплаты)

Для проверки тарифов можно временно выставить уровень подписки пользователю:

- `POST /api/webapp/user/set-subscription`  
  Body: `{ "telegramId": 123456789, "subscriptionLevel": "pro" }`  
  Допустимые значения: `free`, `pro`, `gold`, `premium`.

В production при необходимости задайте переменную окружения `SET_SUBSCRIPTION_SECRET` и передавайте её в заголовке `X-Set-Subscription-Secret` или в теле запроса в поле `secret`, иначе endpoint вернёт 403.

### 1. Клонирование репозитория
```bash
git clone https://github.com/Niki1000/ai-dating-bot.git
cd ai-dating-bot